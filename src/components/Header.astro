---
console.log("Esto es el header");
const pathname = new URL(Astro.request.url).pathname;
---

<header
  class="py-6 flex flex-row justify-center delay-[0ms] duration-[400ms] taos:scale-[1.2] taos:opacity-0"
  data-taos-offset="400"
>
  <div class="flex">
    <nav
      class="flex justify-center rounded-full transition-transform transform hover:scale-105 font-Pacifico"
    >
      <ul class="flex">
        <li>
          <a
            href="/"
            class={pathname === "/"
              ? "text-pink-500 hover:text-pink-500 py-4 px-4"
              : "text-white hover:text-pink-500 py-4 px-4 "}>Inicio</a
          >
        </li>
        <li>
          <a
            href="/works"
            class={pathname === "/works"
              ? "text-pink-500 hover:text-pink-500 py-4 px-4"
              : "text-white hover:text-pink-500 py-4 px-4 "}>Proyectos</a
          >
        </li>
        <li>
          <a
            href={pathname === "/contacto" ? "/:id" : "/#contact"}
            class={pathname === "/contacto"
              ? "text-pink-500 hover:text-pink-500 py-4 px-4"
              : "text-white hover:text-pink-500 py-4 px-4 "}>Contacto</a
          >
        </li>
      </ul>
    </nav>
  </div>
</header>

<style>
  .navbar {
    background-color: #000;
  }
  .navbar a:active {
  }
</style>
